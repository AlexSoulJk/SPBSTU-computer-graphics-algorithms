struct PS_INPUT
{
    float4 pos : SV_Position;
    float4 color : COLOR;
};

float4 main(PS_INPUT input) : SV_Target {
    // Возвращаем цвет с альфа-каналом 0.5 для полупрозрачности
    return float4(input.color.rgb, 0.5f);
}